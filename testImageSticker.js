import { imageToSticker } from "./imageGen.js";

const imagePath = "sample.jpg"; // Change to the path of your test image
const customText = "hackathon kaisa chl rha hai";

imageToSticker(imagePath, customText);
