import { generateImage } from "./imageGen.js";

const prompt = " a duck holding a bat and thratning to beat a person while the person is crying and running away frm the duck in a funny way ";
generateImage(prompt);
